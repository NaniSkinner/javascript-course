// Remember, we're gonna use strict mode in all scripts now!
//"use strict";



const arr = testData[ 17,21,23,12,5,-5,0,4]; 
const days = dayNumber [ 0,1,2,3,4,5,6,7];
printForcast : function testData 
    let (i = 0; i ++);
    console.log(`... $(testData [0]) is $(dayNumber [0] days` ); 



/*
Given an array of forecasted maximum temperatures, the thermometer displays a string with these temperatures.

Example: [17, 21, 23] will print "... 17ºC in 1 days ... 21ºC in 2 days ... 23ºC in 3 days ..."

Create a function 'printForecast' which takes in an array 'arr' and logs a string like the above to the console.

Use the problem-solving framework: Understand the problem and break it up into sub-problems!

TEST DATA 1: [17, 21, 23]
TEST DATA 2: [12, 5, -5, 0, 4]

// 1. Understand the problme 
  - How does a function take in an array? 

// 2. Breaking into sub-problems 
  - Create the Function 
  - Create the arrays (2) 
  - Have the function pull the data from the arrays 
  - Log the string with the data given 
  //

